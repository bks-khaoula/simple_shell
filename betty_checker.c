#include <stdio.h>
#include <stdlib.h>

/**
 * main - this a the main function
 * Return: Always 0 (Success)
*/

int main(void)
{
	int var = 2;

	if (var == 2)
		printf("Statement is True: %d\n", var);
	else
		printf("Statement is False!\n");

	printf("I passed Betty test \n");

	return (0);
}

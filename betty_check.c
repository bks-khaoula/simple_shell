#include <stdio.h>
/**
 * main - this is the main function
 * description - example code that passes the Betty checks
 */
void main(void)
{
	int variable = 0;

	if (variable == 0)
	{
		printf("%d\n", variable);
	}
	printf("I passed Betty checks \n");
}
